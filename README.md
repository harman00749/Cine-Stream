# Cine-Stream
Cine-Stream is a React single page movie explorer built with the TMDB API. It includes popular movies, debounced search, infinite scroll, lazy loaded posters, favorites with localStorage, movie detail pages, and an AI Mood Matcher with fallback suggestions. The app uses clean routing, reusable components, hooks, services, and context.
